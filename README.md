Here's a neatly formatted version of your GitHub `README.md` content, structured like a note:

---

# Git & GitHub Basics – Repository Guide

This repository is created to learn and practice **Git** and **GitHub**.

---

## ✅ Steps to Commit in This Repository

1. **Clone the Repository:**
   ```bash
   git clone https://github.com/taqkla/git-basics-v2.git
   ```

2. **Create/Checkout a Branch:**
   ```bash
   git branch               # To view existing branches
   git checkout branch-name # To switch to an existing branch
   git checkout -b new-branch-name # To create and switch to a new branch
   ```

3. **Make Changes**  
   Update or add your files/code.

4. **Check Status:**
   ```bash
   git status
   # This should show untracked or modified files
   ```

5. **Add Files to Staging:**
   ```bash
   git add .
   ```

6. **Commit the Changes:**
   ```bash
   git commit -m "Your commit message here"
   ```

7. **Push to Remote:**
   ```bash
   git push origin your-branch-name
   ```

8. **Create a Pull Request:**  
   Go to GitHub and create a pull request from your branch to the `master` branch.

---
